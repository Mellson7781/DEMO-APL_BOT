# Telegram Application Bot

**Готовый Telegram-бот для приёма заявок с админ-панелью**

---

## 🚀 Описание

Бот позволяет пользователям оставить заявку на услугу, а администратору удобно управлять заявками прямо в Telegram:

- FSM для поэтапного сбора информации от пользователя  
- Сохранение данных в базе SQLAlchemy  
- Админ-панель: просмотр, фильтрация, изменение статуса  
- Статистика по заявкам  

Этот бот можно быстро адаптировать под любой бизнес или проект.

---

## 🧩 Функционал

### Для пользователя:
- `/start` — приветствие и выбор услуги  
- Ввод имени, контакта и комментария  
- Подтверждение заявки  
- Сохранение данных в БД

### Для администратора:
- `/admin` — админ-панель  
- Просмотр заявок по статусу (`new`, `in_work`, `done`)  
- Изменение статуса заявки  
- Поиск заявки по ID  
- Статистика по количеству заявок  

---

## 🛠 Стек технологий
- Python 3.11+  
- [Aiogram](https://docs.aiogram.dev/) — фреймворк для Telegram  
- SQLAlchemy — ORM для работы с базой данных  
- FSM (Finite State Machine) для логики пользовательских форм  
- SQLite (по умолчанию)  

---

## 📂 Структура проекта

bot/
├── main.py # Точка входа
├── config.py # Настройки и конфигурация
├── database/
│ ├── models.py # Модели базы данных
│ ├── session.py # Создание сессии БД
├── handlers/
│ ├── user.py # Обработчики пользователей
│ ├── admin.py # Обработчики админа
├── states/
│ └── form.py # FSM состояния
├── keyboards/
│ └── inline.py # Inline-клавиатуры
├── services/
│ └── crud.py # CRUD и бизнес-логика
├── .env.example # Шаблон переменных окружения
├── requirements.txt # Зависимости проекта
├── README.md # Документация

yaml
Копировать код

---

## ⚡ Установка и запуск

1. Клонируем репозиторий:
```bash
git clone https://github.com/Mellson7781/DEMO-APL_BOT.git
cd DEMO-APL_BOT

2. Создаём виртуальное окружение:

bash
Копировать код
python -m venv .venv
source .venv/bin/activate  # Linux/Mac
.venv\Scripts\activate     # Windows

3. Устанавливаем зависимости:

bash
Копировать код
pip install -r requirements.txt

4. Создаём .env на основе .env.example:

ini
Копировать код
BOT_TOKEN=YOUR_TOKEN_HERE
ADMIN_ID=YOUR_TELEGRAM_ID
DATABASE_URL=sqlite:///db.sqlite3
5. Запускаем бота:

bash
Копировать код
python main.py